export enum Network {
  TESTNET = 'testnet',
  PUBNET = 'pubnet',
  FUTURENET = 'futurenet',
}

export enum WalletType {
  SECRETKEY = 'secretkey',
  ALBEDO = 'albedo',
  FREIGHTER = 'freighter',
  RABET = 'rabet',
  WALLET_CONNECT = 'walletconnect',
  XBULL = 'xbull',
}

export enum MessageEventType {
  CONNECT = 'onConnect',
  CANCEL = 'onCancel',
  SIGN = 'onSign',
  READY = 'onReady',
}
